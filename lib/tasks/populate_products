# namespace :db do
#   desc "Fetch data from External API and populate model"
#   task populate_products: :environment do
#     url = "https://api.escuelajs.co/api/v1/products"
#     response = Faraday.get(url)

#     if response.success?
#       data = JSON.parse(response.body)

#       data['items'].each do |item|
#         # Use find_or_create_by to avoid duplicate records
#         MyModel.find_or_create_by(external_id: item['id']) do |m|
#           m.title = item['title']
#           m.slug = item['slug']
#           m.price = item['price']
#           m.description = item['description']
#           m.category = item['category']['name']
#         end
#       end
#       puts "Successfully populated #{data['items'].count} records."
#     else
#       puts "Failed to fetch data: #{response.status}"
#     end
#   end
# end